language: node_js
node_js:
  - "lts/*"

services: postgresql

addons:
  postgresql: "9.6"

before_script:
  - psql -c 'CREATE DATABASE ci;' -U postgres

install: 
  - yarn
  - yarn build

script:
  - yarn test
  - NODE_ENV=test yarn migrate-reset